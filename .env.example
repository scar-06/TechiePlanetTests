# Copy to .env and adjust as needed. Every value has a safe local default,
# so docker compose works out of the box without this file.
POSTGRES_DB=studentscores
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
APP_PORT=8080
